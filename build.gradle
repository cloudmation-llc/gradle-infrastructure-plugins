buildscript {
    repositories {
        mavenCentral()
    }
}

plugins {
    id "pl.allegro.tech.build.axion-release" version "1.11.0"
    id "groovy"
    id "maven-publish"
}

scmVersion {
    checks {
        uncommittedChanges = false
    }
}

project.version = scmVersion.version

group "com.cloudmation"

dependencies {
    implementation gradleApi()
    implementation localGroovy()
}

publishing {
    repositories {
        maven {
            name = "GitHubPackages"
            url = project.findProperty("com.cloudmation.ghp.url")
            credentials {
                username = project.findProperty("com.cloudmation.ghp.username")
                password = project.findProperty("com.cloudmation.ghp.token")
            }
        }
    }

    publications {
        gradlePlugin(MavenPublication) {
            artifactId = "gradle-project-plugins"
            from components.java
        }
    }
}